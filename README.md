# inventory-management
inventory management

install the requirements
pip install -r requirements.txt

build executable exe
python -m PyInstaller --onefile --console --name "Inventory Manager" launcher.py
